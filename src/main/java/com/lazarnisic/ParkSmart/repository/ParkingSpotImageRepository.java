package com.lazarnisic.ParkSmart.repository;

import com.lazarnisic.ParkSmart.model.ParkingSpotImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParkingSpotImageRepository extends JpaRepository<ParkingSpotImage, Long> {
}
